{
  "name": "acronymy-workers",
  "version": "0.0.0",
  "devDependencies": {
    "wrangler": "^4.6.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --remote -e dev",
    "deploy": "wrangler deploy -e prod"
  }
}
